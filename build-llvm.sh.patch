--- a/build-llvm.sh
+++ b/build-llvm.sh
@@ -136,6 +136,10 @@ else
     esac
 fi
 
+if [ -d llvm-project ] && [ ! -d llvm-project/llvm/lib/Transforms/Obfuscation ]; then
+  cd llvm-project && patch -p1 < ../obfuscator.patch && cd ..
+fi
+
 if command -v ninja >/dev/null; then
     CMAKE_GENERATOR="Ninja"
 else
